$\xi$为概率空间$(\Omega, \mathscr F, P)$上的随机变量，$F$为$\xi$通过$F(B)=P\{\xi\in B\},B\in\mathscr B$在$\mathscr B$上引出的概率测度，则对于任何 Borel 函数$f(x)$有：
$$
\int_{\mathbb R}f(x)F(dx)=\int_{\Omega}f[\xi(\omega)]P(d\omega)
$$
如果一端有限，则另一端也有限，且二者相等。
>证明：示性函数开始一直到Borel函数

